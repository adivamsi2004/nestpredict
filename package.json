{
  "name": "house-price-pro",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend && cp -r frontend/dist dist",
    "emulators": "npm run emulators --prefix backend"
  }
}
